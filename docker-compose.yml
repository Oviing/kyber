# No compose services: Kyber is local-first. The CLI talks to the local
# Docker daemon directly and manages one container + network + volume per
# sandbox session. Nothing to start here — see `kyber sandbox up`.
#
# Build the sandbox image instead:
#   docker build -f sandbox/images/Dockerfile.sandbox-agent -t kyber-sandbox:latest sandbox/images
# or: kyber sandbox build
